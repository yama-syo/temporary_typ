--- opencv.js	2023-01-04 20:02:31.124177400 +0900
+++ opencv_import.js	2023-01-04 22:21:25.224509300 +0900
@@ -1,3 +1,4 @@
+export var cv;
 (function (root, factory) {
   if (typeof define === 'function' && define.amd) {
     // AMD. Register as an anonymous module.
@@ -11,13 +12,16 @@
     module.exports = factory();
   } else if (typeof window === 'object') {
     // Browser globals
-    root.cv = factory();
+    //root.cv = factory();
+    cv = factory();
   } else if (typeof importScripts === 'function') {
     // Web worker
-    root.cv = factory();
+    //root.cv = factory();
+    cv = factory();
   } else {
     // Other shells, e.g. d8
-    root.cv = factory();
+    //root.cv = factory();
+    cv = factory();
   }
 }(this, function () {
   
@@ -42,7 +46,7 @@
       exports["cv"] = cv;
     
   if (typeof Module === 'undefined')
-    Module = {};
+    var Module = {};
   return cv(Module);
 }));
         
\ No newline at end of file
